{
  "//": "Declared required status checks for this repo (GOL-1735 / GOL-1406-D; ported by GOL-1824).",
  "//audit": "audit-required-merge-group.py asserts every context below is produced by a workflow that triggers on `merge_group` (job name OR a commit status posted via the statuses API), so no required check is PR-only and the merge queue cannot wedge ('Expected — waiting for status'). The weekly reconcile job compares this list against the live required contexts (classic branch protection AND rulesets) and fails on drift, so keep this file in sync when a required check is added or removed.",
  "//gol1953-pause": "Promotion of the guard/hardening layer to required checks (GOL-1953) is PAUSED per board 2026-09-01. This list is kept EQUAL to live branch protection (rulesets) so the GOL-1907 reconcile leg stays green instead of hard-failing on drift. Parked by GOL-2015, mirroring AgenticOS #653. This repo has no .github/merge-policy.json, so the promoted-target set is preserved inline in '//gol1953-parked-promotion-target' below. Promotion will be re-declared here per-repo IN LOCKSTEP with `apply-merge-policy.sh --apply` (Josh) after the GOL-1953 canary. Until then declared == live.",
  "//gol1953-parked-promotion-target": [
    "Protected paths guard",
    "Config freeze",
    "fix touches a test",
    "CI scripts",
    "Required-checks audit",
    "actionlint (all workflows)",
    "shellcheck (scripts/)",
    "Reject tracked .env files",
    "cloud-init ASCII guard",
    "Terraform fmt + validate",
    "Promotion script tests"
  ],
  "branch": "main",
  "required_contexts": [
    "Validate Docker Compose (Grove)",
    "Validate Nginx Config",
    "Lint Python (Odoo Modules)",
    "prod-plan-guard"
  ]
}
